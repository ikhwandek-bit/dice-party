# dice-party

Terminal Yahtzee-style game in C++. See `SPEC.md`.

Build:

```text
cmake -S . -B build
cmake --build build
```

Run the Phase 1 stub:

```text
./build/dice-party
```

On Windows the binary is `build\dice-party.exe` or `build\Debug\dice-party.exe`.